# Deprecated Code

`behaviour_modelling/` contains the pre-leakage-fix behaviour modelling implementation. It is retained only for historical reference and must not be used for training or evaluation. Use `src/dao_governance/` with `scripts/08_run_behaviour_modelling.py`, `scripts/08b_run_behaviour_modelling_no_roberta.py`, and `scripts/09_run_behaviour_evaluation.py` instead.
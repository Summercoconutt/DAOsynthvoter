# Core pipeline
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0
polars>=0.20.0
PyYAML>=6.0
tqdm>=4.66.0
openpyxl>=3.1.0

# ML / DL
torch>=2.1.0
transformers>=4.36.0
scikit-learn>=1.3.0
scipy>=1.11.0

# Clustering / plots
matplotlib>=3.8.0
seaborn>=0.13.0

# Tests (optional)
pytest>=7.4.0

# Large CSV window cache (stage 08b)
duckdb>=1.0.0

# Optional: data expansion (dao_eligible_reconstruct_full.py)
# requests>=2.31.0
# web3>=6.0.0

# Optional: HDBSCAN path in voter clustering
# hdbscan>=0.8.33
